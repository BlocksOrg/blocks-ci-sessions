## Summary

<!-- What changed and why, in your own words. Keep it short. -->

## Linked issue

Fixes #

## How I verified

<!-- Behaviour changes: which tests cover it, and how a reviewer can confirm it.
     Workflow or example changes: a link to a run, or the local mock-API command you used. -->

## Checklist

- [ ] `npm run all` passes (lint, type-check, test, build)
- [ ] `dist/index.mjs` rebuilt and committed if anything under `src/` changed
- [ ] `action.yml` and `README.md` updated if inputs or outputs changed
- [ ] PR title follows Conventional Commits (`type(scope): summary`)
