name: Question
description: Ask a question about using the action
labels: ["question"]
body:
  - type: markdown
    attributes:
      value: |
        Check the [README](https://github.com/BlocksOrg/blocks-ci-sessions#readme) and the [Blocks docs](https://docs.blocks.team) first. For security reports, see SECURITY.md — do not open a public issue.
  - type: textarea
    id: question
    attributes:
      label: Your question
    validations:
      required: true
